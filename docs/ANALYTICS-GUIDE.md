# 📊 Руководство по аналитике studio.ai

## Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Настройка Google Analytics 4](#настройка-google-analytics-4)
3. [Настройка Яндекс.Метрики](#настройка-яндексметрики)
4. [Использование трекинга](#использование-трекинга)
5. [События для отслеживания](#события-для-отслеживания)
6. [GDPR и согласие пользователя](#gdpr-и-согласие-пользователя)

---

## 🚀 Быстрый старт

### 1. Получите идентификаторы

**Google Analytics 4:**
1. Перейдите в [Google Analytics](https://analytics.google.com)
2. Создайте новый ресурс GA4
3. Получите Measurement ID (формат: `G-XXXXXXXXXX`)

**Яндекс.Метрика:**
1. Перейдите в [Яндекс.Метрику](https://metrika.yandex.ru)
2. Создайте новый счетчик
3. Получите ID счетчика (формат: `12345678`)

### 2. Инициализируйте аналитику

Откройте `/App.tsx` и раскомментируйте строку инициализации:

```typescript
// В App.tsx, строка ~29
useEffect(() => {
  document.documentElement.lang = "ru";
  
  // Замените на ваши реальные ID
  initAnalytics('G-XXXXXXXXXX', 12345678);
}, []);
```

### 3. Готово!

Аналитика автоматически начнет отслеживать:
- ✅ Просмотры страниц
- ✅ Переходы между разделами
- ✅ Базовые взаимодействия

---

## 🔧 Настройка Google Analytics 4

### Создание ресурса GA4

1. **Войдите в Google Analytics**
   - Перейдите на https://analytics.google.com
   - Войдите с вашим Google аккаунтом

2. **Создайте аккаунт (если нет)**
   - Нажмите "Администрирование" (⚙️)
   - Выберите "Создать аккаунт"
   - Укажите название: `studio.ai`

3. **Создайте ресурс GA4**
   - В столбце "Ресурс" нажмите "+ Создать ресурс"
   - Название ресурса: `studio.ai Website`
   - Часовой пояс: `Москва (GMT+3)`
   - Валюта: `Российский рубль (RUB)`

4. **Настройте поток данных**
   - Выберите платформу: "Веб"
   - URL сайта: `https://studio.ai`
   - Название потока: `studio.ai Production`

5. **Получите Measurement ID**
   - После создания потока вы увидите ID в формате `G-XXXXXXXXXX`
   - Скопируйте его

### Настройте цели (Goals)

В GA4 создайте следующие события как конверсии:

1. **form_submit** - Отправка формы
2. **cta_click** - Клик на CTA кнопки
3. **ai_consultant_interaction** - Взаимодействие с AI
4. **project_view** - Просмотр проекта
5. **service_interest** - Интерес к услуге

Как создать конверсию:
- Администрирование → События → Создать событие
- Или: Конфигурация → События → Отметить как конверсия

---

## 📈 Настройка Яндекс.Метрики

### Создание счетчика

1. **Войдите в Яндекс.Метрику**
   - Перейдите на https://metrika.yandex.ru
   - Войдите с Яндекс ID

2. **Добавьте счетчик**
   - Нажмите "Добавить счетчик"
   - Адрес сайта: `https://studio.ai`
   - Включите опции:
     - ✅ Вебвизор
     - ✅ Карта кликов
     - ✅ Карта скроллинга
     - ✅ Аналитика форм

3. **Получите ID счетчика**
   - После создания вы увидите числовой ID (например, `12345678`)
   - Скопируйте его

### Настройте цели в Яндекс.Метрике

Перейдите в Настройки → Цели и создайте:

1. **Отправка формы обратной связи**
   - Тип: JavaScript событие
   - Идентификатор: `form_submit`

2. **Клик на CTA**
   - Тип: JavaScript событие
   - Идентификатор: `cta_click`

3. **Просмотр проекта**
   - Тип: JavaScript событие
   - Идентификатор: `project_view`

4. **Взаимодействие с AI**
   - Тип: JavaScript событие
   - Идентификатор: `ai_consultant_interaction`

5. **Глубина просмотра**
   - Тип: Количество просмотров
   - Условие: ≥ 3 страницы

---

## 💻 Использование трекинга

### Базовые функции

```typescript
import {
  trackEvent,
  trackPageView,
  trackButtonClick,
  trackFormSubmit,
  trackProjectView,
  trackServiceInterest,
  trackAIConsultant,
  trackOutboundLink,
} from "./utils/analytics";
```

### Примеры использования

#### 1. Трекинг клика по кнопке

```typescript
// В компоненте Hero.tsx
<button 
  onClick={() => {
    trackButtonClick('Get Started', 'hero');
    // ... ваша логика
  }}
>
  Начать проект
</button>
```

#### 2. Трекинг отправки формы

```typescript
// В ContactForm.tsx
const handleSubmit = async (e) => {
  e.preventDefault();
  
  try {
    await sendForm(formData);
    trackFormSubmit('contact_form', true);
    // Показать success message
  } catch (error) {
    trackFormSubmit('contact_form', false);
    // Показать error message
  }
};
```

#### 3. Трекинг просмотра проекта

```typescript
// В ProjectDetailPage.tsx
useEffect(() => {
  trackProjectView(
    project.id,
    project.title,
    project.category
  );
}, [project]);
```

#### 4. Трекинг AI-консультанта

```typescript
// В AIConsultantPage.tsx
const handleSendMessage = (text: string) => {
  trackAIConsultant('question_asked', text);
  // ... отправка сообщения
};

const handleUnlock = () => {
  trackAIConsultant('chat_unlocked');
  // ... разблокировка чата
};
```

#### 5. Трекинг интереса к услуге

```typescript
// В ServicesPage.tsx
<div 
  onClick={() => {
    trackServiceInterest('Web Development');
    // ... показать детали
  }}
>
  Веб-разработка
</div>
```

#### 6. Трекинг внешних ссылок

```typescript
// В Portfolio.tsx
<a 
  href="https://external-site.com"
  target="_blank"
  onClick={() => trackOutboundLink('https://external-site.com', 'Project Link')}
>
  Посмотреть сайт
</a>
```

#### 7. Кастомные события

```typescript
// Любое кастомное событие
trackEvent('custom_event_name', {
  property1: 'value1',
  property2: 'value2',
  timestamp: Date.now(),
});
```

---

## 🎯 События для отслеживания

### Рекомендуемые события для studio.ai

| Событие | Описание | Где добавить |
|---------|----------|--------------|
| `cta_click` | Клик на главный CTA | Hero, Services, Footer |
| `navigation_click` | Клик по навигации | Navigation |
| `form_submit` | Отправка формы | ContactForm, ProjectContactModal |
| `project_view` | Просмотр проекта | ProjectDetailPage |
| `project_website_click` | Клик на сайт проекта | ProjectDetailPage |
| `service_interest` | Интерес к услуге | ServicesPage |
| `ai_consultant_opened` | Открытие AI-консультанта | AIConsultantPage |
| `ai_consultant_question` | Вопрос AI-консультанту | AIConsultantPage |
| `ai_consultant_unlock` | Разблокировка чата | AIConsultantPage |
| `portfolio_category_click` | Клик по категории портфолио | PortfolioPage |
| `theme_toggle` | Переключение темы | ThemeProvider |
| `social_link_click` | Клик по соцсети | Footer |
| `email_click` | Клик на email | Footer, Contact |
| `scroll_depth_25` | Прокрутка 25% | useScrollDepth hook |
| `scroll_depth_50` | Прокрутка 50% | useScrollDepth hook |
| `scroll_depth_75` | Прокрутка 75% | useScrollDepth hook |
| `scroll_depth_100` | Прокрутка 100% | useScrollDepth hook |

### Пример: Добавление трекинга переключения темы

```typescript
// В ThemeProvider.tsx
const toggleTheme = () => {
  const newTheme = theme === "dark" ? "light" : "dark";
  setTheme(newTheme);
  
  // Трекаем событие
  trackEvent('theme_toggle', {
    from_theme: theme,
    to_theme: newTheme,
  });
};
```

### Пример: Трекинг глубины прокрутки

```typescript
// Создайте хук useScrollDepth.ts
import { useEffect, useState } from 'react';
import { trackScrollDepth } from './utils/analytics';

export function useScrollDepth() {
  const [depths, setDepths] = useState({
    25: false,
    50: false,
    75: false,
    100: false,
  });

  useEffect(() => {
    const handleScroll = () => {
      const scrolled = (window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100;

      if (scrolled >= 25 && !depths[25]) {
        trackScrollDepth(25);
        setDepths(prev => ({ ...prev, 25: true }));
      }
      if (scrolled >= 50 && !depths[50]) {
        trackScrollDepth(50);
        setDepths(prev => ({ ...prev, 50: true }));
      }
      if (scrolled >= 75 && !depths[75]) {
        trackScrollDepth(75);
        setDepths(prev => ({ ...prev, 75: true }));
      }
      if (scrolled >= 100 && !depths[100]) {
        trackScrollDepth(100);
        setDepths(prev => ({ ...prev, 100: true }));
      }
    };

    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, [depths]);
}

// Используйте в App.tsx или в каждой странице
function HomePage() {
  useScrollDepth();
  // ... rest of component
}
```

---

## 🔒 GDPR и согласие пользователя

### Текущая реализация

По умолчанию согласие **автоматически предоставлено**. Это подходит для российского рынка, но для работы с EU нужно запрашивать согласие.

### Добавление cookie banner

Создайте компонент `CookieConsent.tsx`:

```typescript
import { useState, useEffect } from 'react';
import { grantConsent, revokeConsent } from './utils/analytics';

export function CookieConsent() {
  const [show, setShow] = useState(false);

  useEffect(() => {
    const consent = localStorage.getItem('analytics_consent');
    if (!consent) {
      setShow(true);
    }
  }, []);

  const handleAccept = () => {
    grantConsent();
    setShow(false);
  };

  const handleReject = () => {
    revokeConsent();
    setShow(false);
  };

  if (!show) return null;

  return (
    <div className="fixed bottom-0 left-0 right-0 bg-secondary border-t border-border p-6 z-50">
      <div className="max-w-6xl mx-auto flex items-center justify-between gap-4">
        <p className="text-foreground">
          Мы используем cookies и аналитику для улучшения работы сайта. 
          Продолжая использовать сайт, вы соглашаетесь с нашей{' '}
          <a href="/privacy" className="text-accent underline">политикой конфиденциальности</a>.
        </p>
        <div className="flex gap-3">
          <button
            onClick={handleReject}
            className="px-4 py-2 border border-border rounded-xl hover:bg-secondary/50"
          >
            Отклонить
          </button>
          <button
            onClick={handleAccept}
            className="px-4 py-2 bg-accent text-white rounded-xl hover:bg-accent/90"
          >
            Принять
          </button>
        </div>
      </div>
    </div>
  );
}
```

Добавьте в `App.tsx`:

```typescript
import { CookieConsent } from './components/CookieConsent';

export default function App() {
  return (
    <ThemeProvider>
      <div className="min-h-screen bg-background">
        <Navigation />
        {renderPage()}
        <Footer />
        <CookieConsent />
      </div>
    </ThemeProvider>
  );
}
```

---

## 📊 Отчеты и дашборды

### Google Analytics 4

**Основные отчеты для отслеживания:**

1. **Аудитория**
   - Демография
   - Интересы
   - Геолокация
   - Технологии (браузеры, устройства)

2. **Поведение**
   - Просмотры страниц
   - События
   - Конверсии
   - Время на сайте

3. **Источники трафика**
   - Каналы (Organic, Direct, Referral, Social)
   - Источники/Среды
   - Кампании

4. **Конверсии**
   - Отправка форм
   - Клики на CTA
   - Просмотры проектов
   - Взаимодействие с AI

### Яндекс.Метрика

**Полезные инструменты:**

1. **Вебвизор**
   - Записи сеансов пользователей
   - Анализ поведения
   - Выявление проблем UX

2. **Карты**
   - Карта кликов
   - Карта скроллинга
   - Аналитика форм

3. **Отчеты**
   - Посещаемость
   - Источники
   - Контент
   - Конверсии

---

## 🚀 Production Deployment

### Чеклист перед запуском

- [ ] Получены реальные ID для GA4 и Яндекс.Метрики
- [ ] ID добавлены в `App.tsx`
- [ ] Настроены цели/конверсии в обеих системах
- [ ] Добавлен трекинг на все критичные элементы
- [ ] Протестирован трекинг в dev режиме
- [ ] Добавлен cookie consent (если требуется GDPR)
- [ ] Создана политика конфиденциальности
- [ ] Настроены оповещения об аномалиях

### Тестирование

**Google Analytics:**
1. Откройте [Realtime отчет](https://analytics.google.com/analytics/web/#/realtime)
2. Совершите действия на сайте
3. Проверьте что события приходят в реальном времени

**Яндекс.Метрика:**
1. Откройте счетчик → "Сейчас на сайте"
2. Совершите действия
3. Проверьте вебвизор

---

## 📞 Поддержка

Если возникли вопросы по настройке аналитики:
- Email: hello@studio.ai
- Telegram: @studio_ai

---

**Последнее обновление:** 04.11.2025  
**Версия:** 1.0.0
